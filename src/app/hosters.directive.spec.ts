import { HostersDirective } from './hosters.directive';

describe('HostersDirective', () => {
  it('should create an instance', () => {
    const directive = new HostersDirective();
    expect(directive).toBeTruthy();
  });
});
